# build context から除外する対象。
#
# docs/ と scripts/ は除外しない。
# コンテナ内でも検証スクリプトとロードマップを利用できる状態を保つ。

.git
.context
.local

.bundle

# 秘密情報を build context へ持ち込まない。
.env*
config/*.key

log/*
tmp/*
storage/*
public/assets

# Rails 標準の .keep は維持する。
!log/.keep
!tmp/.keep
!tmp/pids/.keep
!tmp/storage/.keep
!storage/.keep
