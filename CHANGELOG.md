# 変更の履歴

この文書は日本語版を正本とする。
版の付け方は [ADR 0053](docs/decisions/0053-versioning-and-release.md) に従う。

## 未リリース

`v0.1.0` の後に `main` へ入ったもの。**まだ版を付けておらず、リリースとしては出していない。**
版番号と公開日は、出すときに決める。

計画していた P0 から P15 までのフェーズは、この時点ですべて完了している。

### 足したもの

| 範囲 | 内容 |
| --- | --- |
| 保存 | 求人の保存、検索条件の保存、保存した条件に合う新着求人の通知（ADR 0054） |
| 候補者検索 | 受信を許可した候補者だけを検索できる。**既定は許可していない**。検索したことは監査ログへ残る（ADR 0055） |
| タレントプール | 組織の中で共有する。表示と件数は、その時点で探せる候補者だけを数える（ADR 0055） |
| スカウト | 送信、テンプレート、組織ごと 1 日 50 通の上限、候補者による組織ごとの配信停止、同じ組織から同じ候補者への重複送信の禁止（ADR 0056） |
| Webhook | 組織ごとに登録し、選んだ種類の出来事だけを配信する。本文は識別子・種類・時刻だけで、個人情報を含まない。署名は HMAC-SHA256。配信の結果を履歴として残す（ADR 0057） |
| CSV | 求人の入出力。外部の識別子で照合し、あれば更新・なければ作成する。取り込んだ求人は必ず下書きにする。1 行の失敗で全体を止めない（ADR 0058） |
| 逆プロキシ | 信頼するプロキシと受け入れるホストを設定できるようにし、起動時に前提を検証する（ADR 0062） |
| 入力 | 受け取る要求の大きさに上限を設ける（ADR 0063） |

判断の記録（ADR）は 63 件になった（`docs/decisions/`）。

### 直したもの

セキュリティと堅牢性の修正。いずれも `main` へ反映済みである。

| 範囲 | 内容 |
| --- | --- |
| レート制限 | 招待とメッセージの回数を、認証と必要な認可を通る前に数えていた。IP 単位の共有の枠を、認可を通らない要求で減らせた |
| 招待 | 招待の作成に回数の上限がなく、送信キューへ積み続けられた |
| Webhook | 配信先を内部ネットワークへ向けられた。旧サイトローカルの IPv6 も含めて拒否する（ADR 0060） |
| CSV | 書き出した値が表計算ソフトで数式として解釈されうる形のまま出ていた（ADR 0061） |
| 組織 | 並行した操作で、組織の管理者が 0 人になりうる状態があった |
| スカウト | 並行した送信で 1 日の上限を超えられた。判定と保存を組織ごとに直列化する |
| タレントプール | 受信の許可を取り消した候補者が、表示と件数に残っていた |
| 公開 | sitemap が条件付き GET で 500 を返す場合があった |

### 速くしたもの

| 範囲 | 内容 |
| --- | --- |
| 一覧 | preload した関連を使わずに読み直していた経路をなくした |
| 公開 | 求人一覧と sitemap が同じ問い合わせを繰り返していた |
| 通知 | 既読の記録を 1 通ずつ作らず、未読の判定を反結合にした |
| 保持期限 | 増え続ける表へ索引を足し、期限切れの削除を分割して行う |

### まだ入っていないもの

- iCalendar の入出力（P14 の対象範囲に挙げたが、完了条件に含めていない）
- CRM・カレンダーのアダプター境界（同上）。個別サービス向けの実装は非目標のままである
- メールとファイルの保存先は Rails の設定で切り替える。専用のアダプター層は持たない
- メッセージの添付
- 常設の公開環境、コンテナイメージの配布
- 候補者・応募者の CSV（**意図的に持たない**。ADR 0058）

### 既知の制限

`v0.1.0` から引き継いでいるもの、およびこの範囲で増えたもの。

| 制限 | 詳細 |
| --- | --- |
| 性能 | sitemap とキーワード検索が件数とともに遅くなる。**実測は `v0.1.0` 時点のままで、上の「速くしたもの」の後に測り直していない**（`docs/performance/load-test-results.md`） |
| 検索 | 求人・候補者とも `ILIKE` による部分一致。表記のゆれ・同義語を扱わない |
| 通知 | 配信の失敗は運営の画面を見に行かないと気付けない |
| 添付 | PDF のみ。ウイルス検査はしない |
| 保持期限 | 自動では実行されない。rake タスクを運用で呼ぶ |
| 監査ログ | 改ざんの検出はしない |
| スカウト | 送信の上限（1 日 50 通）は運用の実績に基づく値ではない |
| Webhook | 送った先には公開範囲も保持期限も効かない。宛先の管理は利用者の責任である |
| アクセシビリティ | 色のコントラスト比と支援技術での実操作は未検証 |
| 運営者 | 権限の付与（rake タスク）は記録に残らない |
| 監査 | 侵入テストと第三者による監査は受けていない |

すべて `docs/quality/verification-status.md` と
`docs/security/threat-model.md` に記載している。

## v0.1.0 - 2026-08-02

最初のリリース。**評価できる状態**であり、本番運用を保証するものではない。

### 入っているもの

| 範囲 | 内容 |
| --- | --- |
| 基盤 | Rails 8.1 / PostgreSQL 18 / Docker Compose、統一検証（`bin/verify`） |
| 画面 | ロケール付きの URL（日本語・英語）、共通レイアウト、静的エラー画面 |
| アカウント | 登録、メールの確認、ログイン、パスワード再設定、認証の記録 |
| 組織 | 組織の作成、招待、役割（管理者・一般）、権限の変更の履歴、運営者 |
| 求人 | 作成・編集、申請と承認の分離、公開状態の履歴、公開時の表示 |
| 公開 | 求人の一覧・詳細、キーワードと条件の絞り込み、ページ分割、sitemap・robots、構造化データ |
| プロフィール | 基本情報、職歴、学歴、スキル、希望条件、履歴書の添付、公開範囲 |
| 応募 | 応募、二重応募の防止、応募時点の写し、取消、企業側からの閲覧 |
| 選考 | 選考の段階、評価とコメント、担当者、面接の予定、結論の期限 |
| 連絡 | 応募に紐づく会話、既読、アプリ内通知、メール通知、配信の失敗と再送 |
| 安全 | レート制限、CSP、保持期限、削除と匿名化、読んだ操作の監査ログ |
| 運用 | 構造化ログ、遅い SQL の記録、負荷試験、容量モデル、バックアップ手順 |
| 評価 | 架空データのデモ、ロール別の手順、アーキテクチャの概要 |

判断の記録（ADR）は 53 件ある（`docs/decisions/`）。

### 入っていないもの

v0.1.0 の必須条件にしていない。

- スカウト（企業から求職者への働きかけ）
- 汎用の外部連携（ATS・カレンダー・チャット）
- 候補者の検索・一覧（**意図的に持たない**。ADR 0030）
- メッセージの添付
- 常設の公開環境、コンテナイメージの配布

### 既知の制限

| 制限 | 詳細 |
| --- | --- |
| 性能 | sitemap とキーワード検索が件数とともに遅くなる（`docs/performance/load-test-results.md`） |
| 検索 | `ILIKE` による部分一致。表記のゆれ・同義語を扱わない |
| 通知 | 配信の失敗は運営の画面を見に行かないと気付けない |
| 添付 | PDF のみ。ウイルス検査はしない |
| 保持期限 | 自動では実行されない。rake タスクを運用で呼ぶ |
| 監査ログ | 改ざんの検出はしない |
| アクセシビリティ | 色のコントラスト比と支援技術での実操作は未検証 |
| 運営者 | 権限の付与（rake タスク）は記録に残らない |

すべて `docs/quality/verification-status.md` と
`docs/security/threat-model.md` に記載している。

### 動かすには

[README](README.md) の「セットアップ」を参照する。
`docker compose run --rm app bin/rails roleweave:demo:seed` で架空データを入れられる。
