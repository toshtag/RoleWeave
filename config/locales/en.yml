en:
  # モデル名と属性名は、検証エラーの文言と画面の項目名の両方から参照される。
  # 片方だけで訳すと、同じ項目が場所によって違う名前で表示される。
  activerecord:
    models:
      user: Account
    attributes:
      user:
        email_address: Email address
        password: Password
        password_confirmation: Password confirmation
    # bcrypt が切り捨てる長さは文字数ではなくバイト数で決まる。
    # 既定の文言は文字数を前提にしているため、この検証専用の文言を持つ。
    errors:
      models:
        user:
          attributes:
            password:
              too_long_in_bytes: must be at most %{count} bytes
  application:
    name: RoleWeave
    skip_to_content: Skip to main content
    language_switcher:
      label: Display language
      # 言語名は訳さず、その言語自身の表記で書く。
      # 訳すと、その言語しか読めない閲覧者が自分の言語を見つけられない。
      languages:
        ja: 日本語
        en: English
    footer:
      description: An open-source project building a self-hostable recruiting platform.
  home:
    show:
      summary: We are building the foundation of a self-hostable recruiting platform.
