<%#
  本文はテキストだけとする。装飾を持たせると、表示できる形式が受信環境に依存する。
  パスワードそのものは書かない。再設定はリンクから利用者自身が行う。
%><%= t(".greeting") %>

<%= t(".body") %>

<%= @password_reset_url %>

<%= t(".expiration", count: @expires_in_minutes) %>

<%= t(".ignore") %>

--
<%= t("application.name") %>
