<% content_for :title, "#{t(".title")} | #{t("application.name")}" %>

<h1><%= t(".title") %></h1>

<%= render "form",
           skill: @skill,
           url: profile_skill_path(locale: I18n.locale, id: @skill),
           method: :patch,
           submit_label: t(".submit") %>
