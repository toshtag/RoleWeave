<% content_for :title, "#{t(".title")} | #{t("application.name")}" %>

<h1><%= t(".title") %></h1>

<p><%= t(".body") %></p>

<p><%= link_to t(".organizations"), organizations_path(locale: I18n.locale) %></p>
