<% content_for :title, "#{t(".title")} | #{t("application.name")}" %>

<h1><%= t(".title", organization: @organization.name) %></h1>

<%= render "form",
           job_posting: @job_posting,
           url: organization_job_postings_path(locale: I18n.locale, organization_id: @organization),
           submit_label: t(".submit") %>
